import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const Friends = (props) => {
    return(
        <SafeAreaView>
            <Text>Friends</Text>
        </SafeAreaView>
    );
}

export default Friends;